#ifndef MANDELBROT_ALGORITHM_TYPES
#define MANDELBROT_ALGORITHM_TYPES

typedef void escaeTimeAlgorithm(int** escapeTimes, int max_iters, int sizeX, int sizeY, double scale, double panX, double panY);

#endif