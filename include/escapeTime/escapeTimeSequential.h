#ifndef ESCAPE_TIME_SEQUENTIAL_PUBLIC
#define ESCAPE_TIME_SEQUENTIAL_PUBLIC

void escapeTimeSequential(int* escapeTimes, int maxIters, int sizeX, int sizeY, double scale, double panX, double panY);

#endif 